name: Tactical Alert Cron

on:
  schedule:
    # 08:45 UTC corrisponde alle 10:45 italiane
    - cron: '45 8 * * *'
  workflow_dispatch: # Permette il test manuale

jobs:
  ping_server:
    runs-on: ubuntu-latest
    steps:
      - name: Bussata al server Altervista
        run: |
          curl -s "https://centrosicurezzazava.altervista.org/cron_telegram.php?key=SVEGLIA_TACTICAL_2026"
